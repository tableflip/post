form(method="post" action="/routes")#create
  label(for="referer").label Post from
  input(type="url" name="referer" id="referer").input
  
  label(for="email").label Email to
  input(type="email" name="email" id="email").input
  
  label(for="referer").label Redirect on sent
  input(type="text" name="redirect" id="redirect" value="?sent=true").input
  
  label(for="redirectError").label Redirect on error
  input(type="text" name="redirectError" id="redirectError" value="?sent=false").input
  
  button(type="submit").btn.btn-primary Save
